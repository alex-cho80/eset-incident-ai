#!/usr/bin/env bash
set -euo pipefail

echo "Rotate ESET credentials, Discord webhook URL, and sanitizer HMAC secret in the configured secret manager."
echo "Do not print secret values in this script."
