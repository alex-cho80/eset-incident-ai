name: Evaluation

on:
  pull_request:

permissions:
  contents: read

jobs:
  eval-placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Validate eval fixtures exist
        run: test -f evals/datasets/prompt_injection_cases.jsonl
